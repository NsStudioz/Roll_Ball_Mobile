using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class TrapConfig : MonoBehaviour
{

    void Update()
    {
        // transform.Translate(Vector2.zero * Time.deltaTime, Space.World);
        // transform.Rotate(-Vector3.right * Time.deltaTime);
        // transform.rotation = Quaternion.Lerp(transform.rotation, transform.rotation, 100 * Time.deltaTime);
        // Quaternion target = Quaternion.Euler(0, 0, 60);
       // transform.Rotate(new Vector3(0, 0, Time.deltaTime * 5));

    }
}
